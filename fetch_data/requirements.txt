# fetch_data 数据采集模块依赖
# 安装: pip install -r requirements.txt

# 通用依赖（所有脚本均需）
pandas>=2.0
numpy>=1.24

# AkShare 数据源（免费，无需 Token）
akshare>=1.10

# Tushare Pro 数据源（需 TUSHARE_TOKEN 环境变量）
tushare>=1.3

# QMT / miniQMT 数据源
# xtquant 随 QMT 客户端发布（离线 whl 包），不在公共 PyPI 上
# 安装方式：pip install <路径>/xtquant-*.whl
# xtquant

# 可选：环境变量管理（方便加载 .env 中的 TUSHARE_TOKEN）
# python-dotenv
